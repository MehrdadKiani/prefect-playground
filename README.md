## prefect --help
```
prefect server start
prefect agent start default-agent-pool

prefect cloud login
prefect cload logout

prefect profile ls
prefect profile inspect default
prefect profile use default
```